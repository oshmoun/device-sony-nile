PRIO=1,1,1,1,0,0,0,0
#arch_timer, arch_mem_timer, glink-smem-rpm, arm-pmu, mpm irqs
IGNORED_IRQ=3,5,149,165,312

# adb logcat -sdv color MSM-irqbalance:* | grep banned | awk '{print substr($10,4)}' | sort -un
